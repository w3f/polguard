environment: development

redis:
  url: redis://redis:6379/0

matrix:
  serverAddress: "https://matrix.web3.foundation"
  userId: "@user:web3.foundation"
  # Password could be also provided via env variable MATRIX_PASSWORD
  password: "password"
  # Default logging level is warn for matrix sdk, console logs are not suppresed for debug level only
  logging:
    level: warn

  # Rooms for the bot functionality
  rooms:
    # TestMonitoringPlatform room
    - id: "!BabNKVNdOktKGafXzd:web3.foundation"

logging:
  level: info