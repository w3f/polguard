environments:
  ci:
  local:
  test:

repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami

releases:

- name: redis
  {{ if eq .Environment.Name "test" }}
  namespace: test
  {{ end }}
  chart: bitnami/redis
  version: 20.9.0
  values:
    - ./config/redis-values.yaml.gotmpl
