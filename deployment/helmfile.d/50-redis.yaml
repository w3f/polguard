environments:
  ci:
  local:
  test:

repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami

releases:

- name: redis
  {{ if eq .Environment.Name "test" }}
  namespace: test
  {{ end }}
  chart: bitnami/redis
  version: ^20.2.1
  values:
    - ./config/redis-values.yaml.gotmpl
