environments:
  ci:
  local:
  test:

repositories:
- name: w3f
  url: https://w3f.github.io/helm-charts/

releases:

- name: api
  {{ if eq .Environment.Name "test" }}
  namespace: test
  {{ end }}
  chart: ../charts/api
  values:
  - ./config/api-values.yaml.gotmpl
