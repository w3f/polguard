environments:
  ci:
  local:
  test:

repositories:
- name: w3f
  url: https://w3f.github.io/helm-charts/

releases:

- name: watcher
  {{ if eq .Environment.Name "test" }}
  namespace: test
  {{ end }}
  chart: ../charts/watcher
  values:
  - ./config/watcher-values.yaml.gotmpl

- name: watcher-people
  {{ if eq .Environment.Name "test" }}
  namespace: test
  {{ end }}
  chart: ../charts/watcher
  values:
  - ./config/watcher-people-values.yaml.gotmpl