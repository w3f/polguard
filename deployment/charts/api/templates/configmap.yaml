apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.name" . }}
data:
  config.yaml: |-
{{ toYaml .Values.config | indent 4 }}

--- 
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.name" . }}-monitoring
data:
  config.yaml: |-
{{ toYaml .Values.monitoringConfig | indent 4 }}
