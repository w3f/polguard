environment: production

# Database configuration
database:
  host: postgres
  port: 5432
  username: postgres
  password: postgres
  database: incident_management

# HTTP server configuration
server:
  port: 3000
  host: 0.0.0.0

# Notification service configuration
notification:
  matrix:
    url: http://matrix:3000/api/notify

# Logging configuration
logging:
  level: info
