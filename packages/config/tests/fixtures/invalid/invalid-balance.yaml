accountSets:
  invalid-balances:
    - address: "5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY"
      selfStake: "1.2.3"          # Invalid decimal format

groups:
  - id: invalid-balances
    chains:
      - Polkadot
    notifications:
      messengerType: Matrix
      channels: ['!room:matrix.org']
    monitors:
      - name: Balances
        threshold: "invalid.number"  # Invalid decimal format
        handlers:
          - BalanceThreshold
    accountSetNames: [invalid-balances]
