# Monitors defined but notifications missing in both defaults and group
accountSets:
  missing-required-accounts:
    - address: "5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY"

defaults:
  chains:
    - Polkadot
  monitors:
    - name: Staking
      commission: 10
      handlers:
        - CommissionChangedEvent

groups:
  - id: missing-required-fields-group
    accountSetNames: [missing-required-accounts]
