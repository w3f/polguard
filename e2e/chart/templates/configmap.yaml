apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-configs
  labels:
    {{ include "common.labels.standard" . | nindent 4 }}
data:
  config.yaml: |-
{{ toYaml .Values.tests.config | indent 4 }}
