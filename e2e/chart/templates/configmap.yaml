apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-configs
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  config.yaml: |-
{{ toYaml .Values.tests.config | indent 4 }}
