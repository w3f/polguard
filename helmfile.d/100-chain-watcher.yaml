environments:
  ci:
  local:
  test:

repositories:
- name: w3f
  url: https://w3f.github.io/helm-charts/

releases:

- name: chain-watcher
  namespace: test
  chart: ../charts/chain-watcher
  values:
  - ./config/watcher-values.yaml.gotmpl
