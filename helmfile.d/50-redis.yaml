environments:
  ci:
  local:
  test:

repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami

releases:

- name: redis
  namespace: test
  chart: bitnami/redis
  version: ^20.2.1
  values:
    - ./config/redis-values.yaml.gotmpl
