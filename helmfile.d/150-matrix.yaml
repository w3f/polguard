environments:
  ci:
  local:
  staging:

repositories:
- name: w3f
  url: https://w3f.github.io/helm-charts/

releases:

- name: matrix
  chart: ../charts/matrix
  values:
  - ./config/matrix-values.yaml.gotmpl
